function carregarAtividade2(){
 
    let totalSoma = 0;
    let numero = 1;
    while (numero <= 50) {
        totalSoma = totalSoma + numero;
        numero++;
        console.log(`A soma total é: ${totalSoma}`);

    }
}